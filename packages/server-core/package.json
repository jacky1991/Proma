{
  "name": "@proma/server-core",
  "version": "0.3.13",
  "license": "AGPL-3.0-only",
  "description": "Proma 核心引擎共享包：从 Electron 主进程剥离的零 Electron 依赖逻辑（Agent 会话 / 渠道 / 工作区 / 对话管理）。Electron 桌面端与 Web 服务端共用此真源。",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./node": "./src/node.ts",
    "./*": "./src/*.ts",
    "./adapters/*": "./src/adapters/*.ts",
    "./chat-tools/*": "./src/chat-tools/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@proma/shared": "workspace:*",
    "@proma/core": "workspace:*",
    "iconv-lite": "^0.6.3"
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "0.80.9",
    "@earendil-works/pi-ai": "0.80.9",
    "@earendil-works/pi-coding-agent": "0.80.9"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
